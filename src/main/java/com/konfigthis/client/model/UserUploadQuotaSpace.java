/*
 * Vimeo API
 * Build something great. Vimeo's API supports flexible, high-quality video integration with your custom apps.
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.konfigthis.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import org.apache.commons.lang3.StringUtils;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.konfigthis.client.JSON;

/**
 * Information about the authenticated user&#39;s upload space remaining for the current period.
 */
@ApiModel(description = "Information about the authenticated user's upload space remaining for the current period.")@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class UserUploadQuotaSpace {
  public static final String SERIALIZED_NAME_FREE = "free";
  @SerializedName(SERIALIZED_NAME_FREE)
  private Double free;

  public static final String SERIALIZED_NAME_MAX = "max";
  @SerializedName(SERIALIZED_NAME_MAX)
  private Double max;

  /**
   * The type of quota for the values of the **upload_quota.space** field.  Option descriptions:  * &#x60;lifetime&#x60; - The quota type is lifetime.  * &#x60;periodic&#x60; - The quota type is periodic. 
   */
  @JsonAdapter(ShowingEnum.Adapter.class)
 public enum ShowingEnum {
    LIFETIME("lifetime"),
    
    PERIODIC("periodic");

    private String value;

    ShowingEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static ShowingEnum fromValue(String value) {
      for (ShowingEnum b : ShowingEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<ShowingEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final ShowingEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public ShowingEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return ShowingEnum.fromValue(value);
      }
    }
  }

  public static final String SERIALIZED_NAME_SHOWING = "showing";
  @SerializedName(SERIALIZED_NAME_SHOWING)
  private ShowingEnum showing;

  /**
   * The unit that&#39;s used to compute quota.  Option descriptions:  * &#x60;video_count&#x60; - The quota is calculated using the count of the videos.  * &#x60;video_size&#x60; - The quota is calculated using the byte size of the videos. 
   */
  @JsonAdapter(UnitEnum.Adapter.class)
 public enum UnitEnum {
    COUNT("video_count"),
    
    SIZE("video_size");

    private String value;

    UnitEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static UnitEnum fromValue(String value) {
      for (UnitEnum b : UnitEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      return null;
    }

    public static class Adapter extends TypeAdapter<UnitEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final UnitEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public UnitEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return UnitEnum.fromValue(value);
      }
    }
  }

  public static final String SERIALIZED_NAME_UNIT = "unit";
  @SerializedName(SERIALIZED_NAME_UNIT)
  private UnitEnum unit;

  public static final String SERIALIZED_NAME_USED = "used";
  @SerializedName(SERIALIZED_NAME_USED)
  private Double used;

  public UserUploadQuotaSpace() {
  }

  public UserUploadQuotaSpace free(Double free) {
    
    
    
    
    this.free = free;
    return this;
  }

  public UserUploadQuotaSpace free(Integer free) {
    
    
    
    
    this.free = free.doubleValue();
    return this;
  }

   /**
   * The number of bytes or videos remaining in the authenticated user&#39;s upload quota.
   * @return free
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(example = "5000000", required = true, value = "The number of bytes or videos remaining in the authenticated user's upload quota.")

  public Double getFree() {
    return free;
  }


  public void setFree(Double free) {
    
    
    
    this.free = free;
  }


  public UserUploadQuotaSpace max(Double max) {
    
    
    
    
    this.max = max;
    return this;
  }

  public UserUploadQuotaSpace max(Integer max) {
    
    
    
    
    this.max = max.doubleValue();
    return this;
  }

   /**
   * The maximum number of bytes or videos allotted to the authenticated user&#39;s upload quota.
   * @return max
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "5368709120", required = true, value = "The maximum number of bytes or videos allotted to the authenticated user's upload quota.")

  public Double getMax() {
    return max;
  }


  public void setMax(Double max) {
    
    
    
    this.max = max;
  }


  public UserUploadQuotaSpace showing(ShowingEnum showing) {
    
    
    
    
    this.showing = showing;
    return this;
  }

   /**
   * The type of quota for the values of the **upload_quota.space** field.  Option descriptions:  * &#x60;lifetime&#x60; - The quota type is lifetime.  * &#x60;periodic&#x60; - The quota type is periodic. 
   * @return showing
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(example = "LIFETIME", required = true, value = "The type of quota for the values of the **upload_quota.space** field.  Option descriptions:  * `lifetime` - The quota type is lifetime.  * `periodic` - The quota type is periodic. ")

  public ShowingEnum getShowing() {
    return showing;
  }


  public void setShowing(ShowingEnum showing) {
    
    
    
    this.showing = showing;
  }


  public UserUploadQuotaSpace unit(UnitEnum unit) {
    
    
    
    
    this.unit = unit;
    return this;
  }

   /**
   * The unit that&#39;s used to compute quota.  Option descriptions:  * &#x60;video_count&#x60; - The quota is calculated using the count of the videos.  * &#x60;video_size&#x60; - The quota is calculated using the byte size of the videos. 
   * @return unit
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "VIDEO_COUNT", required = true, value = "The unit that's used to compute quota.  Option descriptions:  * `video_count` - The quota is calculated using the count of the videos.  * `video_size` - The quota is calculated using the byte size of the videos. ")

  public UnitEnum getUnit() {
    return unit;
  }


  public void setUnit(UnitEnum unit) {
    
    
    
    this.unit = unit;
  }


  public UserUploadQuotaSpace used(Double used) {
    
    
    
    
    this.used = used;
    return this;
  }

  public UserUploadQuotaSpace used(Integer used) {
    
    
    
    
    this.used = used.doubleValue();
    return this;
  }

   /**
   * The number of bytes or videos that the authenticated user has already uploaded against their quota.
   * @return used
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(example = "5368704120", required = true, value = "The number of bytes or videos that the authenticated user has already uploaded against their quota.")

  public Double getUsed() {
    return used;
  }


  public void setUsed(Double used) {
    
    
    
    this.used = used;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the UserUploadQuotaSpace instance itself
   */
  public UserUploadQuotaSpace putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UserUploadQuotaSpace userUploadQuotaSpace = (UserUploadQuotaSpace) o;
    return Objects.equals(this.free, userUploadQuotaSpace.free) &&
        Objects.equals(this.max, userUploadQuotaSpace.max) &&
        Objects.equals(this.showing, userUploadQuotaSpace.showing) &&
        Objects.equals(this.unit, userUploadQuotaSpace.unit) &&
        Objects.equals(this.used, userUploadQuotaSpace.used)&&
        Objects.equals(this.additionalProperties, userUploadQuotaSpace.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(free, max, showing, unit, used, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UserUploadQuotaSpace {\n");
    sb.append("    free: ").append(toIndentedString(free)).append("\n");
    sb.append("    max: ").append(toIndentedString(max)).append("\n");
    sb.append("    showing: ").append(toIndentedString(showing)).append("\n");
    sb.append("    unit: ").append(toIndentedString(unit)).append("\n");
    sb.append("    used: ").append(toIndentedString(used)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("free");
    openapiFields.add("max");
    openapiFields.add("showing");
    openapiFields.add("unit");
    openapiFields.add("used");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("free");
    openapiRequiredFields.add("max");
    openapiRequiredFields.add("showing");
    openapiRequiredFields.add("unit");
    openapiRequiredFields.add("used");
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to UserUploadQuotaSpace
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!UserUploadQuotaSpace.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in UserUploadQuotaSpace is not found in the empty JSON string", UserUploadQuotaSpace.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : UserUploadQuotaSpace.openapiRequiredFields) {
        if (jsonObj.get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonObj.toString()));
        }
      }
      if (!jsonObj.get("showing").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `showing` to be a primitive type in the JSON string but got `%s`", jsonObj.get("showing").toString()));
      }
      if (!jsonObj.get("unit").isJsonNull() && !jsonObj.get("unit").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `unit` to be a primitive type in the JSON string but got `%s`", jsonObj.get("unit").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!UserUploadQuotaSpace.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'UserUploadQuotaSpace' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<UserUploadQuotaSpace> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(UserUploadQuotaSpace.class));

       return (TypeAdapter<T>) new TypeAdapter<UserUploadQuotaSpace>() {
           @Override
           public void write(JsonWriter out, UserUploadQuotaSpace value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additonal properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   obj.add(entry.getKey(), gson.toJsonTree(entry.getValue()).getAsJsonObject());
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public UserUploadQuotaSpace read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             // store additional fields in the deserialized instance
             UserUploadQuotaSpace instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of UserUploadQuotaSpace given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of UserUploadQuotaSpace
  * @throws IOException if the JSON string is invalid with respect to UserUploadQuotaSpace
  */
  public static UserUploadQuotaSpace fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, UserUploadQuotaSpace.class);
  }

 /**
  * Convert an instance of UserUploadQuotaSpace to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

